id: claude
name: Claude Code
version: 1

primary_memory:
  path: "~/.claude/memory/shared.md"
  mode: managed_section
  marker: shared

allowlist:
  - "~/.claude/memory"

cold_start:
  install_mode: index_pointer
  recommended_path: "~/.claude/CLAUDE.md"

skills:
  target_template: "~/.claude/skills/<skill>/SKILL.md"

safety:
  reject_symlink_outside_allowlist: true
  backup_before_write: true
